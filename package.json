{
  "name": "ai-slop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "100% AI가 만들고 자동으로 연재하는 만화와 영상",
  "scripts": {
    "import:wind": "node scripts/import-wind-series.mjs",
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs",
    "check": "npm run build && npm run verify",
    "serve": "npx --yes serve _site"
  },
  "engines": {
    "node": ">=22"
  }
}
